/* Set these when using Supabase persistence; leave empty for local-only (Live Server). */
window.SAGE_SUPABASE_URL = window.SAGE_SUPABASE_URL || '';
window.SAGE_SUPABASE_ANON_KEY = window.SAGE_SUPABASE_ANON_KEY || '';
